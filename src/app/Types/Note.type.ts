export type Note = {
  noteId: string;
  noteContent: any;
  noteIndex: number;
  noteColor: string;
  noteCreationDate: string;
  noteUpdateDate: string;
  noteTitle: string;
  active: boolean;
}
