<mat-dialog-content class="flex flex-col p-2">
  <h1 class="mb-2">Insert the URL</h1>
  <input
    class="p-1 outline-none active:outline-none border border-gray-200 rounded-md"
    [(ngModel)]="url"
    placeholder="URL"
  />
</mat-dialog-content>
<mat-dialog-actions>
  <button
    (click)="onNoClick()"
    class="m-1 p-1 px-3 rounded-full hover:bg-slate-200"
  >
    {{ "components.insertion-modal.cancelButton" | translate}}
  </button>
  <button
    [mat-dialog-close]="url()"
    class="m-1 p-1 px-3 rounded-full hover:bg-slate-200"
  >
    {{ "components.insertion-modal.okButton" | translate}}
  </button>
</mat-dialog-actions>
